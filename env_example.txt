# Rename this file to .env and fill in your values

# VertexAI Mode Configuration
GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Service Account Credentials (required for Agent Engine creation)
# Option A: Set path to service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
# Option B: Use 'gcloud auth application-default login' instead

# GCP Project and Location (required)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Agent Engine ID (created via setup_agent_engine.py)
# Required for VertexAiSessionService and VertexAiMemoryBankService
# Run: python setup_agent_engine.py to create one
AGENT_ENGINE_ID=your-agent-engine-id

